# CMS Frontend

Next.js app for public posts/albums and admin content management.

## Setup

1. Copy `.env.local.example` to `.env.local` and update values.
2. Install dependencies with `npm install`.
3. Run `npm run dev`.
