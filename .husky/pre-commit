#!/bin/sh

# Quick smoke checks for the Lightsail CMS rewrite
# Full test suite runs in CI

set -e

echo "🔍 Running pre-commit checks..."

if [ -d "heron" ]; then
  echo "🧪 ESLint (heron)"
  if [ -d "heron/node_modules" ]; then
    (cd heron && npm run lint)
  else
    echo "⚠️  heron/node_modules missing, run npm install"
  fi
else
  echo "⚠️  heron not found, skipping frontend lint"
fi

echo "✅ Pre-commit checks complete"
